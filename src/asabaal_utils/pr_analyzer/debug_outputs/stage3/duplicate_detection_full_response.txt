## CRITICAL:
- `content/blog-processing-scripts/extract-and-transform-blog-posts.py` and `content/blog-processing-scripts/extract-blog-posts.py` - Both are Python scripts for extracting blog posts, likely performing similar or identical functions
- `content/blog-processing-scripts/transform-blog-posts.py` and `content/blog-processing-scripts/transform-blog-posts-2.py` - Both are Python scripts for transforming blog posts, likely performing similar or identical functions
- `content/blog-processing-scripts/transform-blog-posts-3.py` and `content/blog-processing-scripts/transform-blog-posts-2.py` - Both are Python scripts for transforming blog posts, likely performing similar or identical functions

## HIGH:
- `content/blog-processing-scripts/extract-and-transform-blog-posts.py` and `content/blog-processing-scripts/extract-blog-posts.py` - Both are Python scripts for extracting blog posts, likely performing similar or identical functions
- `content/blog-processing-scripts/transform-blog-posts.py` and `content/blog-processing-scripts/transform-blog-posts-2.py` - Both are Python scripts for transforming blog posts, likely performing similar or identical functions
- `content/blog-processing-scripts/transform-blog-posts-3.py` and `content/blog-processing-scripts/transform-blog-posts-2.py` - Both are Python scripts for transforming blog posts, likely performing similar or identical functions

## MEDIUM:
- `content/blog-processing-scripts/extract-and-transform-blog-posts.py` and `content/blog-processing-scripts/transform-blog-posts.py` - Both are Python scripts for processing blog posts, but one extracts and the other transforms, suggesting they may be part of a pipeline
- `content/blog-processing-scripts/extract-blog-posts.py` and `content/blog-processing-scripts/transform-blog-posts.py` - Both are Python scripts for processing blog posts, but one extracts and the other transforms, suggesting they may be part of a pipeline
- `content/blog-processing-scripts/transform-blog-posts-2.py` and `content/blog-processing-scripts/transform-blog-posts-3.py` - Both are Python scripts for transforming blog posts, but may be different versions of the same functionality

## Summary:
The duplicate analysis reveals several files with overlapping functionality, particularly in the `content/blog-processing-scripts/` directory where multiple scripts perform similar extraction and transformation tasks. There are also potential duplicates in database setup files and deployment guides, though these require more detailed content analysis to confirm. The critical duplicates are the multiple blog processing scripts that likely perform identical or very similar functions. The high duplicates are the same scripts that are duplicated with slight variations in naming, suggesting they are either different versions or experimental implementations. The medium duplicates are scripts that perform related but distinct tasks (extraction vs transformation) and may be part of a pipeline. The content samples indicate these are all Python scripts for processing blog content, which supports the finding of functional duplicates. The database setup files and deployment guides require more detailed analysis to confirm if they are truly duplicates or just similar in purpose. The content samples show that the database setup files are likely for setting up a Supabase database with similar functionality, while the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be a duplicate. The blog processing scripts are the most likely candidates for functional duplicates. The content samples show that the blog processing scripts are all Python scripts for processing blog posts, and the filenames suggest they are different versions or implementations of the same functionality. The database setup files are likely for setting up a Supabase database with similar functionality, and the deployment guides are for deploying to Vercel, which may be similar but not identical. The visualizations are likely different visualizations of similar concepts, but without more detailed analysis, it's hard to say if they are truly duplicates or just similar in theme. The start-server.sh script is a simple bash script for starting a local server, and the content samples show it's a simple script, so it's unlikely to be